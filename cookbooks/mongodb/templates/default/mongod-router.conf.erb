systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongos.log

net:
  port: 27017
  bindIp: <%= @ips.join(',') %>

sharding:
  configDB: <%= @replica_name %>/<%= @hosts %>

security:
  authorization: enabled
