systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongos.log

net:
  port: 27017
  bindIp: <%= @ip %>,127.0.0.1

sharding:
  configDB: <%= @replica_name %>/<%= @hosts %>

security:
  authorization: enabled
